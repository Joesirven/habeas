> inherits: ../AGENTS.md

# AGENTS.md — app/intake_drop_poller/

**Kind:** automation

Scheduled poll of California DELETE Act API; feature-flagged off until API is live.


- Vendor adapter code in `adapters/` inside this app only.
- Schema in [`db/migrations/`](../../db/migrations/) — prefix `intake_` or `matching_` as appropriate.
