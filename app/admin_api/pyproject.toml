[project]
name = "admin-api"
version = "0.1.0"
requires-python = ">=3.12"
dependencies = [
    "habeas-privacy-core",
    "data-fulfillment-dispatcher",
    "fastapi>=0.115",
    "google-auth>=2.36",
    "google-cloud-bigquery>=3.25",
    "httpx>=0.28",
    "paramiko>=3.4",
    "python-multipart>=0.0.9",
    "uvicorn[standard]>=0.32",
    "sse-starlette>=2.0",
]

[tool.uv.sources]
habeas-privacy-core = { workspace = true }
data-fulfillment-dispatcher = { workspace = true }

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/admin_api"]
