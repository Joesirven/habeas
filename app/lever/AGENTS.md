> inherits: ../AGENTS.md

# AGENTS.md — app/lever/

**Kind:** automation

Candidate matching and archive/opt-out suppression via Lever API.


- Vendor adapter code in `adapters/` inside this app only.
- Schema in [`db/migrations/`](../../db/migrations/) — prefix `lever_` or `matching_` as appropriate.
