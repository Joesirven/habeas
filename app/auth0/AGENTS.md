> inherits: ../AGENTS.md

# AGENTS.md — app/auth0/

**Kind:** automation

User matching and block/revoke suppression via Auth0 Management API.


- Vendor adapter code in `adapters/` inside this app only.
- Schema in [`db/migrations/`](../../db/migrations/) — prefix `auth0_` or `matching_` as appropriate.
