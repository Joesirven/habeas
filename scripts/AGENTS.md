> inherits: ../AGENTS.md

# AGENTS.md — scripts/

Ad-hoc operational scripts. **Not imported** by `libs/`, `app/`, or `clients/`.

Runnable from developer laptops or automation — one-off migrations helpers, local setup, data fixes (with Jose approval).

Do not put reusable production logic here — put it in `habeas-privacy-core`.
