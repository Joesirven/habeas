# Copy to profiles.yml in this directory (gitignored) or point DBT_PROFILES_DIR here.
#
#   cp profiles.yml.example profiles.yml
#   DBT_PROFILES_DIR=. dbt build
#
# Auth: Application Default Credentials (gcloud auth application-default login)
# or a service account with BigQuery jobUser + dataEditor on access_export /
# access_export_stg and dataViewer on person_db.
#
# Do not commit profiles.yml or credentials.

access_export:
  target: dev
  outputs:
    dev:
      type: bigquery
      method: oauth
      project: "{{ env_var('DBT_GCP_PROJECT', 'example-gcp-project') }}"
      dataset: access_export
      location: us-east4
      threads: 4
      job_execution_timeout_seconds: 3600
      priority: interactive
